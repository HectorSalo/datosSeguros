<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skysam.datossegurosFirebaseFinal">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".common.DatosSeguros"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DatosSegurosYellow">
        <activity android:name=".launcher.ui.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".settings.BloqueoActivity"/>
        <activity
            android:name=".settings.AcercaActivity"
            android:theme="@style/Theme.Acerca" />
        <activity android:name=".generalActivitys.EditarActivity" />
        <activity android:name=".launcher.ui.RegistrarActivity" />
        <activity
            android:name=".launcher.ui.InicSesionActivity"
            android:theme="@style/Theme.NoActionBar" />
        <activity
            android:name=".generalActivitys.AddActivity"
            android:label="@string/title_activity_add" />
        <activity
            android:name=".generalActivitys.MainActivity"
            android:label="@string/app_name" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>